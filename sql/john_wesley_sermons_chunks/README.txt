Run these files in order inside Supabase SQL Editor:

1. 00_schema.sql
2. 01_reset.sql
3. seed_part_01.sql ... seed_part_44.sql

Important:
- Open one file at a time, run it, wait for success, then move to the next file.
- Do not paste multiple seed files into one SQL Editor query tab.

This split avoids SQL Editor payload-size limits.
